// Site-wide business settings. Fill these in before showing the site to
// customers: the "Book a capture" / "Get in touch" buttons stay hidden until
// one of them is set.
export const SITE = {
  // e.g. 'hello@ukemixr.com' - opens the visitor's mail app.
  contactEmail: '',
  // e.g. a booking page or form URL; used instead of the email when set.
  contactUrl: '',
};
